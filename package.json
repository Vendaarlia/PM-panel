{
  "name": "client-handoff-dashboard",
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "dev": "astro dev",
    "build": "astro build",
    "preview": "bun run build && wrangler dev",
    "dbgen": "drizzle-kit generate",
    "dbmig": "drizzle-kit migrate",
    "dbpus": "drizzle-kit push",
    "migrate:tenants": "bun run scripts/migrate-tenants.ts",
    "provision:tenant": "bun run scripts/provision-tenant.ts",
    "link:tenant": "bun run scripts/link-tenant.ts",
    "deploy": "bun run build && wrangler deploy",
    "cf-typegen": "wrangler types"
  },
  "dependencies": {
    "@astrojs/cloudflare": "12",
    "@astrojs/vercel": "^9.0.0",
    "@astrojs/vue": "^5.0.0",
    "@libsql/client": "^0.14.0",
    "@types/bcryptjs": "^3.0.0",
    "astro": "^5.0.0",
    "bcryptjs": "^3.0.3",
    "bun-sqlite-migrations": "^1.0.0",
    "dotenv": "^17.4.2",
    "drizzle-orm": "^0.45.2",
    "vue": "^3.4.0"
  },
  "devDependencies": {
    "@types/better-sqlite3": "^7.6.13",
    "@types/bun": "^1.3.12",
    "better-sqlite3": "^12.9.0",
    "drizzle-kit": "^0.31.10",
    "wrangler": "^4.84.0"
  }
}
